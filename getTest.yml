config:
  target: 'http://localhost:3003'
  phases:
    - duration: 60
      arrivalRate: 1000    
scenarios:
  - flow:
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"  
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"  
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"  
    - get:
        url: "/api/amenities/{{ $randomNumber(1,8888888) }}"  


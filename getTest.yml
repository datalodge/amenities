config:
  target: 'http://localhost:3003'
  http:
    pool: 10  
  phases:
    - duration: 10
      arrivalRate: 1000    
scenarios:
  - flow:
    - get:
        url: "/api/amenities/{{ $randomNumber(1,10000000) }}"
    - get:
        url: "/api/amenities/{{ $randomNumber(1,10000000) }}"
    - get:
        url: "/api/amenities/{{ $randomNumber(1,10000000) }}"
    - get:
        url: "/api/amenities/{{ $randomNumber(1,10000000) }}"  
    - get:
        url: "/api/amenities/{{ $randomNumber(1,10000000) }}"  


